---
apiVersion: v1
kind: Secret
metadata:
  name: idgenerator-prod-spring
type: Opaque
stringData:
  user: SPRING.SECURITY.USER.NAME
  password: SPRING.SECURITY.USER.PASSWORD
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: prometheus
  name: idgenerator-prod-metrics
spec:
  endpoints:
    - interval: 15s
      path: /actuator/prometheus
      targetPort: 8080
      basicAuth:
        password:
          name: idgenerator-prod-spring
          key: password
        username:
          name: idgenerator-prod-spring
          key: user
  namespaceSelector:
    matchNames:
      - idgenerator-prod
  selector:
    matchLabels:
      app.kubernetes.io/name: idgenerator-prod
