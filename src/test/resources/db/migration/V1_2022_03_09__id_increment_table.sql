CREATE TABLE ID_INCREMENT
(
    ID              NUMBER(20,0) GENERATED BY DEFAULT AS IDENTITY,
    BATCH           INTEGER NOT NULL,
    GENERATOR_CODE  VARCHAR(255) UNIQUE,
    INCREMENT_VALUE NUMBER(20,0),
    PRIMARY KEY (id)
);
